## setting up

* export python env

```python
pip list --format=freeze > requirements.txt
```

* import python env
```python
pip install -r requirements.txt
```

* view docs
```python
mkdocs serve
```